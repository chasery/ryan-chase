import React from "react";
import styled from "styled-components";

const HeaderWrapper = styled.header`
    background-color: white;
`;

const Header = () => {
    return <HeaderWrapper>Testing</HeaderWrapper>;
};

export default Header;
